### Pattern golang project

Just clone this repo and enjoy the bootstrap project

#### Install package 
`go mod vendor`

#### Run the main app
`go run main.go`

#### If you use `air`
`air -c .air.toml`